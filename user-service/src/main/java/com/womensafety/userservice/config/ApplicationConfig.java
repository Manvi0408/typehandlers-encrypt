package com.womensafety.userservice.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class ApplicationConfig {
    // Configuration removed to allow service to start without Redis
    // Redis caching will be added back later when infrastructure is stable
}